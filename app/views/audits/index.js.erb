$("#activities-dialog").html('<%= escape_javascript(render("shared/audit_trail", :record => @trip_ticket, :no_header => true)) %>');
$("#activities-dialog").dialog({
  title: "Change History",
  width: 1062,
  modal: true,
  buttons: {
    Cancel: function() {
      $( this ).dialog( "close" );
    }
  }
});
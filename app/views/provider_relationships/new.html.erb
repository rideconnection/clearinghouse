<div class="padded">
  <header>
    <h2>New Provider Relationship</h2>
  </header>

  <section class="form-holder">
    <%= render 'form' %>
  </section>
</div>

<% content_for :sidebar do %>
  <ul class="side-nav">
    <% if @current_user.provider.present? %>
      <li><%= link_to 'Provider', @current_user.provider %></li>
    <% else %>
      <li><%= link_to 'All Providers', providers_path %></li>
    <% end %>
  </ul>
<% end %>


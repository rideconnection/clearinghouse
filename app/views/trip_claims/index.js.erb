$("#modal-dialog").html('<%= escape_javascript(render("trip_tickets/trip_claims", :trip_claims => @trip_claims)) %>');
$("#modal-dialog").dialog({
  title: "Claims",
  width: 1062,
  modal: true,
  draggable: false,
  resizable: false,
  buttons: {
    OK: function() {
      $( this ).dialog( "close" );
    }
  }
});
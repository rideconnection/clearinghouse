$("#activities-dialog").html('<%= escape_javascript(render("trip_tickets/trip_claims", :trip_claims => @trip_claims)) %>');
$("#activities-dialog").dialog({
  title: "Claims",
  width: 1062,
  modal: true,
  buttons: {
    Cancel: function() {
      $( this ).dialog( "close" );
    }
  }
});
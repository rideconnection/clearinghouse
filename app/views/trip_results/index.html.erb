<h1>Listing trip_results</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trip_results.each do |trip_result| %>
  <tr>
    <td><%= link_to 'Show', trip_result %></td>
    <td><%= link_to 'Edit', edit_trip_result_path(trip_result) %></td>
    <td><%= link_to 'Destroy', trip_result, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Trip result', new_trip_result_path %>

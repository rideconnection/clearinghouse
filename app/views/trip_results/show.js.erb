$("#modal-dialog").html('<%= escape_javascript(render("show_fields", :result => @trip_result)) %>');
$("#modal-dialog").dialog({
  title: "Result",
  width: 1062,
  modal: true,
  draggable: false,
  resizable: false,
  buttons: {
    OK: function() {
      $( this ).dialog( "close" );
    }
  }
});